<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.utbm.lo52.taaroaffbad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity" android:label="LardeSports">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.VolantActivity"
            android:label="Stocks"/>
        <activity android:name=".Activity.VolantPageActivity"
            android:label="Volant"/>
        <activity android:name=".Activity.VenteActivity"
            android:label="Achats"/>
        <activity android:name=".Activity.VentePageActivity"
            android:label="Commande"></activity>
    </application>

</manifest>